const initialState = {
  uuid: 0,
  email: 'yi-ling.huang@hopebaytech.com',
  displayName: 'Elaine',
};

const user = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default user;